using System;
namespace {{Params.CoreProjectName}}.Interfaces
{
    public interface ITransactionScopeFactory
    {
        ITransactionScope BeginTransaction();
    }
    public interface ITransactionScope : IDisposable
    {
        void Commit();
        void Rollback();
    }
}
